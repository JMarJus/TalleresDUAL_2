<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
	<head>
		<meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />
		<title>Spring 01 - View person</title>
		<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
		<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
	</head>
	<body class="card-body">
		<div class="card text-white bg-dark mb-3">
			<div class="card-header">List of all the apartments in the building</div>
			<div class="card-body">
				<table class="table table-striped table-dark">
					<thead class="thead-light">
						<tr>
							<th scope="col">ID</th>
							<th scope="col">Floor ID</th>
							<th scope="col">Letter</th>
							<th scope="col">- Delete -</th>
						</tr>
					</thead>
					<tbody>
						<th:block th:each="apartment:${apartments}">
							<tr>
								<td th:text="${apartment.id}"/>
								<td th:text="${apartment.floorId}"/>
								<td th:text="${apartment.letter}"/>
								<td>
									<form th:action="@{/postRemoveApartment}" method="post">
										<button type="submit" id="btnRemovePerson" name="id" th:value="${apartment.id}" class="btn btn-danger">X</button>
									</form>
								</td>
							</tr>
						</th:block>
					</tbody>
				</table>
			</div>
		</div>
	</body>
</html>